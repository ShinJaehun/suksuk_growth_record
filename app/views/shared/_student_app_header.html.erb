<section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"
         data-student-profile-card>
  <%= render "shared/student_identity",
        student: current_student,
        classroom: current_student.classroom %>
</section>

<%= render "shared/growth_dashboard_navigation",
      active: active,
      today_path: student_growth_path(tab: "today"),
      growth_path: student_growth_path(tab: "growth") %>
