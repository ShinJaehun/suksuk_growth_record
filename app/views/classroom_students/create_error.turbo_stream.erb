<%= turbo_stream.replace "modal" do %>
  <%= render partial: "classroom_students/form",
        locals: {
          classroom: @classroom,
          student: @student,
          return_to: return_to_context
        } %>
<% end %>
